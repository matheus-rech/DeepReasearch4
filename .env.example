# Example environment configuration for the Deep Research screener

# API key for the OpenAI Deep Research API.  Required to launch screening jobs.
OPENAI_API_KEY=sk-your-openai-api-key

# Optional: override the default SQLite database with a PostgreSQL URL
# DATABASE_URL=postgresql://user:password@localhost:5432/citations

# Optional: base URL of the MCP server.  Include scheme and port but omit
# the trailing `/sse/` – it will be appended automatically by the UI.
# MCP_URL=http://localhost:8001

# Optional: choose a Deep Research model version.  Defaults to the latest
# supported model if unset.  See deep_research.py for details.
# DEEP_RESEARCH_MODEL=o3-deep-research-2025-06-26